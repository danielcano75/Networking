# Networking
A modular and API-agnostic networking framework for iOS built with Swift. It provides a generic HTTP client, configurable request pipeline, response validation, and generic decoding while keeping all API-specific details in the host application.
